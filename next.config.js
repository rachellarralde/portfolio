/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "a.storyblok.com",
      "shop.output.com",
      "output.com",
      "images.unsplash.com",
      "missingbrontosaur.us",
    ],
  },
};

module.exports = nextConfig;
